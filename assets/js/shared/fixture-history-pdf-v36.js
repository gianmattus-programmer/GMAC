(()=>{
'use strict';
const $=(q,el=document)=>el.querySelector(q);
const $$=(q,el=document)=>[...el.querySelectorAll(q)];
const clean=v=>String(v??'').replace(/\s+/g,' ').trim();
const safeName=v=>clean(v||'gmac').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'').slice(0,90)||'gmac';
const upper=v=>clean(v).normalize('NFD').replace(/[\u0300-\u036f]/g,'').toUpperCase();
const finalText=()=>upper($('[data-detail-status]')?.textContent).includes('FINALIZADO');
function toast(message){let el=$('.gm-share-toast');if(!el){el=document.createElement('div');el.className='gm-share-toast';document.body.appendChild(el)}el.textContent=message;el.classList.add('is-visible');clearTimeout(toast.t);toast.t=setTimeout(()=>el.classList.remove('is-visible'),3200)}
function syncFixtureButtons(){$$('[data-share-capture="fixture"]').forEach(btn=>{btn.dataset.pdfReady='36';btn.textContent='DESCARGAR FIXTURE PDF ↓';btn.setAttribute('aria-label','Descargar fixture completo en PDF vectorial')})}
function syncFinalizedButtons(){if(!document.body.classList.contains('gm-detail-page')||!finalText())return;$$('[data-detail-register]').forEach(btn=>{btn.disabled=false;btn.removeAttribute('disabled');btn.removeAttribute('aria-disabled');btn.innerHTML='VER FIXTURE COMPLETO <span>➜</span>';btn.setAttribute('aria-label','Ver fixture completo con todos los resultados')})}
function makeReadonlyModal(){const modal=$('.gm-modal');if(!modal)return;modal.classList.add('gm-modal--readonly');const tag=$('.gm-modal__head .gm-tag',modal);if(tag)tag.textContent='HISTORIAL DEL TORNEO';const note=$('.gm-note',modal);if(note)note.hidden=true;const code=$('#access-code',modal);if(code?.closest('.gm-field'))code.closest('.gm-field').hidden=true;const feedback=$('[data-feedback]',modal);if(feedback)feedback.hidden=true;const reg=$('.gm-registration',modal);if(reg)reg.hidden=true;setTimeout(()=>{const msg=$('[data-fixture-message]',modal);if(msg)msg.textContent='Edición finalizada · fixture histórico con todos los participantes y resultados oficiales.';if(document.activeElement===code)code.blur();window.GMAC_ENHANCE_BRACKETS?.()},450)}
function openFinalizedFixture(btn){const id=btn.dataset.register||new URLSearchParams(location.search).get('id');const original=window.GM_OPEN_TOURNAMENT;if(!id||typeof original!=='function'){toast('No se pudo abrir el fixture histórico.');return}const nativeFind=Array.prototype.find;let touched=null,oldStatus;Array.prototype.find=function(predicate,thisArg){const found=nativeFind.call(this,predicate,thisArg);if(found&&String(found.id)===String(id)){touched=found;oldStatus=found.status;found.status='Vigente'}return found};try{const pending=original(id);makeReadonlyModal();Promise.resolve(pending).then(()=>{makeReadonlyModal();window.GMAC_ENHANCE_BRACKETS?.()}).catch(err=>{console.error('[GMAC historial]',err);toast('No se pudo cargar el fixture histórico.')})}finally{Array.prototype.find=nativeFind;if(touched)touched.status=oldStatus}}
function latin(v){return clean(v).replace(/[–—]/g,'-').replace(/[“”]/g,'"').replace(/[‘’]/g,"'").replace(/→/g,'>').replace(/←/g,'<').replace(/↗/g,'').replace(/↓/g,'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^\x20-\xFF]/g,'?')}
const escPdf=v=>latin(v).replace(/\\/g,'\\\\').replace(/\(/g,'\\(').replace(/\)/g,'\\)');
const n=v=>Number(v).toFixed(2).replace(/\.00$/,'');
function editionLabel(){const raw=clean($('[data-detail-edition]')?.textContent||$('[data-selected-tournament]')?.textContent);const m=raw.match(/EDICI[ÓO]N\s*(\d+)/i)||raw.match(/(\d+)\s*(?:ERA|DA|TA)?\s*EDICI/i);if(!m)return'EDICION';const x=Number(m[1]),s=x===1?'ERA':x===2?'DA':x===3?'ERA':'TA';return`${x}${s} EDICION`}
function matchRows(match){return $$('.gm-v36-player',match).map(row=>({name:clean($('.gm-v36-player__name',row)?.textContent)||'Por definir',score:clean($('.gm-v36-score',row)?.textContent).replace('—',''),penalty:clean($('.gm-v36-penalty',row)?.textContent)}))}
async function trophyJpeg(bracket){const src=$('.gm-v36-trophy img',bracket)?.src;if(!src)return null;try{const r=await fetch(src,{cache:'force-cache'});if(!r.ok)return null;const blob=await r.blob();const bmp=await createImageBitmap(blob);const size=260,canvas=document.createElement('canvas');canvas.width=size;canvas.height=size;const ctx=canvas.getContext('2d');ctx.fillStyle='#f4f3ee';ctx.fillRect(0,0,size,size);const scale=Math.min((size-20)/bmp.width,(size-20)/bmp.height),w=bmp.width*scale,h=bmp.height*scale;ctx.drawImage(bmp,(size-w)/2,(size-h)/2,w,h);bmp.close?.();const jpg=await new Promise(resolve=>canvas.toBlob(resolve,'image/jpeg',.92));if(!jpg)return null;const bytes=new Uint8Array(await jpg.arrayBuffer());let binary='';for(let i=0;i<bytes.length;i+=8192)binary+=String.fromCharCode(...bytes.subarray(i,i+8192));return{binary,width:size,height:size}}catch(e){return null}}
async function createPdf(panel){
  window.GMAC_ENHANCE_BRACKETS?.();await new Promise(r=>requestAnimationFrame(()=>requestAnimationFrame(r)));
  const bracket=$('[data-gmac-bracket]',panel);const PW=1190.55,PH=841.89,M=34,CONTENT_W=PW-M*2;const pages=[];let ops=[],pageNo=0;const rgb=h=>{const s=h.replace('#','');return[0,2,4].map(i=>parseInt(s.slice(i,i+2),16)/255)};const col={bg:rgb('#18191a'),panel:rgb('#272829'),paper:rgb('#f4f3ee'),ink:rgb('#111214'),muted:rgb('#a7a8aa'),line:rgb('#696c71'),blue:rgb('#315fd6'),pen:rgb('#22356d')};const fill=c=>`${n(c[0])} ${n(c[1])} ${n(c[2])} rg`;const stroke=c=>`${n(c[0])} ${n(c[1])} ${n(c[2])} RG`;const yPdf=(top,h=0)=>PH-top-h;const rect=(x,top,w,h,c,sc=null)=>{ops.push(`${fill(c)} ${n(x)} ${n(yPdf(top,h))} ${n(w)} ${n(h)} re f`);if(sc)ops.push(`${stroke(sc)} .75 w ${n(x)} ${n(yPdf(top,h))} ${n(w)} ${n(h)} re S`)};const line=(x1,t1,x2,t2,c=col.line,w=.8)=>ops.push(`${stroke(c)} ${n(w)} w ${n(x1)} ${n(PH-t1)} m ${n(x2)} ${n(PH-t2)} l S`);const text=(v,x,top,size=10,bold=false,c=col.paper)=>{const s=escPdf(v);if(!s)return;ops.push(`${fill(c)} BT /${bold?'F2':'F1'} ${n(size)} Tf 1 0 0 1 ${n(x)} ${n(PH-top-size)} Tm (${s}) Tj ET`)};const fit=(v,max,size=10)=>{const s=latin(v),limit=Math.max(4,Math.floor(max/(size*.52)));return s.length>limit?s.slice(0,Math.max(1,limit-1))+'...':s};
  const title=clean($('[data-selected-tournament]',panel.closest('.gm-modal'))?.textContent||$('[data-detail-title]')?.textContent||'Torneo GMAC');
  function header(sub='FIXTURE Y RESULTADOS OFICIALES'){rect(0,0,PW,PH,col.bg);text('GMAC',M,24,14,true,col.paper);text('FIXTURE OFICIAL',M,45,24,true,col.paper);text(fit(title,CONTENT_W-250,12),M,78,12,true,col.paper);text(sub,M,98,8,true,col.muted);text(`PAGINA ${pageNo+1}`,PW-M-78,28,8,true,col.muted);line(M,116,PW-M,116,col.line,1)}
  let cup=null;
  if(bracket){cup=await trophyJpeg(bracket);header('CUADRO ELIMINATORIO · PARTIDO UNICO');renderBracket(bracket)}else{header();renderFallback()}
  pages.push(ops.join('\n'));
  const objects=[];const add=s=>{objects.push(s);return objects.length};const font1=add('<< /Type /Font /Subtype /Type1 /BaseFont /Helvetica /Encoding /WinAnsiEncoding >>');const font2=add('<< /Type /Font /Subtype /Type1 /BaseFont /Helvetica-Bold /Encoding /WinAnsiEncoding >>');let imageId=0;if(cup)imageId=add(`<< /Type /XObject /Subtype /Image /Width ${cup.width} /Height ${cup.height} /ColorSpace /DeviceRGB /BitsPerComponent 8 /Filter /DCTDecode /Length ${cup.binary.length} >>\nstream\n${cup.binary}\nendstream`);const contentIds=[],pageIds=[];pages.forEach(content=>{contentIds.push(add(`<< /Length ${content.length} >>\nstream\n${content}\nendstream`));pageIds.push(add('PENDING_PAGE'))});const pagesId=objects.length+1,catalogId=objects.length+2;pageIds.forEach((pid,i)=>{const xo=imageId?` /XObject << /Cup ${imageId} 0 R >>`:'';objects[pid-1]=`<< /Type /Page /Parent ${pagesId} 0 R /MediaBox [0 0 ${PW} ${PH}] /Resources << /Font << /F1 ${font1} 0 R /F2 ${font2} 0 R >>${xo} >> /Contents ${contentIds[i]} 0 R >>`});add(`<< /Type /Pages /Kids [${pageIds.map(id=>`${id} 0 R`).join(' ')}] /Count ${pageIds.length} >>`);add(`<< /Type /Catalog /Pages ${pagesId} 0 R >>`);let pdf='%PDF-1.4\n%âãÏÓ\n',offsets=[0];objects.forEach((obj,i)=>{offsets[i+1]=pdf.length;pdf+=`${i+1} 0 obj\n${obj}\nendobj\n`});const xref=pdf.length;pdf+=`xref\n0 ${objects.length+1}\n0000000000 65535 f \n`;for(let i=1;i<=objects.length;i++)pdf+=`${String(offsets[i]).padStart(10,'0')} 00000 n \n`;pdf+=`trailer\n<< /Size ${objects.length+1} /Root ${catalogId} 0 R >>\nstartxref\n${xref}\n%%EOF`;const bytes=new Uint8Array(pdf.length);for(let i=0;i<pdf.length;i++)bytes[i]=pdf.charCodeAt(i)&255;return new Blob([bytes],{type:'application/pdf'});
  function renderMatch(x,top,w,h,match){const rows=matchRows(match);rect(x,top,w,h,col.panel,col.line);const rowH=h/2;rows.slice(0,2).forEach((r,idx)=>{if(idx)line(x,top+rowH,x+w,top+rowH,[.25,.26,.27],.45);const hasPen=r.penalty!=='';const scoreW=22,penW=hasPen?22:0,gap=4,nameW=w-16-scoreW-penW-(hasPen?gap:0);rect(x+5,top+idx*rowH+4,nameW,rowH-8,col.paper);text(fit(r.name,nameW-10,7),x+10,top+idx*rowH+9,7,true,col.ink);rect(x+6+nameW,top+idx*rowH+4,scoreW,rowH-8,col.paper);text(r.score||'-',x+12+nameW,top+idx*rowH+9,8,true,col.ink);if(hasPen){rect(x+6+nameW+scoreW+gap,top+idx*rowH+4,penW,rowH-8,col.pen,col.blue);text(r.penalty,x+13+nameW+scoreW+gap,top+idx*rowH+9,8,true,col.paper)}})}
  function renderBracket(br){const left=$$('.gm-v36-round[data-side="left"]',br).sort((a,b)=>Number(a.dataset.roundIndex)-Number(b.dataset.roundIndex));const right=$$('.gm-v36-round[data-side="right"]',br);const R=left.length;if(!R){renderFallback();return}const gap=10,finalW=245,roundW=Math.max(72,(CONTENT_W-finalW-gap*(2*R))/(2*R));const centerX=M+R*(roundW+gap),areaTop=162,areaH=596,matchH=44;const positions={left:{},right:{}};left.forEach((colEl,idx)=>{const x=M+idx*(roundW+gap),matches=$$('[data-v36-match]',colEl),titleTxt=clean($('.gm-v36-round__title',colEl)?.textContent);text(fit(titleTxt,roundW,10),x,130,10,true,col.paper);positions.left[idx]=matches.map((m,i)=>{const cy=areaTop+(i+.5)*areaH/matches.length,top=cy-matchH/2;renderMatch(x,top,roundW,matchH,m);return{x,top,cy,w:roundW,el:m}})});for(let idx=0;idx<R;idx++){const colEl=right.find(x=>Number(x.dataset.roundIndex)===idx);if(!colEl)continue;const x=centerX+finalW+gap+(R-1-idx)*(roundW+gap),matches=$$('[data-v36-match]',colEl),titleTxt=clean($('.gm-v36-round__title',colEl)?.textContent);text(fit(titleTxt,roundW,10),x,130,10,true,col.paper);positions.right[idx]=matches.map((m,i)=>{const cy=areaTop+(i+.5)*areaH/matches.length,top=cy-matchH/2;renderMatch(x,top,roundW,matchH,m);return{x,top,cy,w:roundW,el:m}})}for(const side of['left','right'])for(let r=0;r<R-1;r++){const from=positions[side][r]||[],to=positions[side][r+1]||[];from.forEach((a,i)=>{const b=to[Math.floor(i/2)];if(!b)return;const x1=side==='left'?a.x+a.w:a.x,x2=side==='left'?b.x:b.x+b.w,mid=(x1+x2)/2;line(x1,a.cy,mid,a.cy);line(mid,a.cy,mid,b.cy);line(mid,b.cy,x2,b.cy)})}const final=$('[data-v36-final] [data-v36-match]',br);const finalTop=areaTop+areaH/2-67;rect(centerX,finalTop-54,finalW,178,col.paper,[.48,.49,.5]);text('FINAL',centerX+12,finalTop-42,18,true,col.ink);text(editionLabel(),centerX+12,finalTop-18,10,true,[.32,.33,.35]);const trophyX=centerX+104,trophyY=finalTop-42,trophyS=74;rect(trophyX,trophyY,trophyS,trophyS,[1,1,1],[.68,.68,.66]);if(cup){ops.push(`q ${n(trophyS-8)} 0 0 ${n(trophyS-8)} ${n(trophyX+4)} ${n(PH-(trophyY+4)-(trophyS-8))} cm /Cup Do Q`)}else text('COPA',trophyX+22,trophyY+31,8,true,col.muted);const fmX=centerX+12,fmY=finalTop+42,fmW=finalW-24;renderMatch(fmX,fmY,fmW,58,final);const lf=(positions.left[R-1]||[])[0],rf=(positions.right[R-1]||[])[0];if(lf){const mid=(lf.x+lf.w+centerX)/2;line(lf.x+lf.w,lf.cy,mid,lf.cy);line(mid,lf.cy,mid,fmY+15);line(mid,fmY+15,fmX,fmY+15)}if(rf){const x2=centerX+finalW,mid=(rf.x+x2)/2;line(rf.x,rf.cy,mid,rf.cy);line(mid,rf.cy,mid,fmY+43);line(mid,fmY+43,fmX+fmW,fmY+43)}const third=$('.gm-v36-third-place [data-v36-match]',br);if(third){text('TERCER PUESTO',centerX+72,finalTop+116,7,true,col.muted);renderMatch(centerX+35,finalTop+130,finalW-70,42,third)}text('GMAC · DOCUMENTO VECTORIAL · FIXTURE OFICIAL',M,PH-28,8,true,col.muted)}
  function renderFallback(){let cursor=145;const rounds=$$('.gm-league-round',panel);if(rounds.length){rounds.forEach((round,ri)=>{const matches=$$('.gm-league-match',round);if(cursor+32+matches.length*30>PH-45){pages.push(ops.join('\n'));ops=[];pageNo++;header('CALENDARIO OFICIAL');cursor=145}text(clean($('summary b',round)?.textContent)||`JORNADA ${ri+1}`,M,cursor,13,true,col.paper);cursor+=25;matches.forEach(m=>{const teams=$$('.gm-league-match__teams b',m).map(x=>clean(x.textContent));const score=clean($('.gm-league-match__teams span',m)?.textContent||'VS');rect(M,cursor,CONTENT_W,25,col.panel,col.line);text(fit(teams[0]||'Por definir',360,9),M+10,cursor+7,9,true,col.paper);text(score,PW/2-24,cursor+7,9,true,col.paper);text(fit(teams[1]||'Por definir',360,9),PW/2+55,cursor+7,9,true,col.paper);cursor+=30})});return}text('El fixture eliminatorio todavía no está disponible.',M,160,13,true,col.paper);text('Cuando se complete la fase correspondiente aparecerá aquí.',M,184,9,false,col.muted)}
}
function downloadPdf(blob,name){const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;a.rel='noopener';a.style.display='none';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),12000)}
document.addEventListener('click',e=>{const btn=e.target.closest('[data-detail-register]');if(!btn||!finalText())return;e.preventDefault();e.stopImmediatePropagation();openFinalizedFixture(btn)},true);
document.addEventListener('click',async e=>{const btn=e.target.closest('[data-share-capture="fixture"]');if(!btn)return;e.preventDefault();e.stopImmediatePropagation();const panel=btn.closest('.gm-fixture-panel');if(!panel){toast('No se encontró el fixture para exportar.');return}const old=btn.textContent;btn.disabled=true;btn.textContent='GENERANDO PDF…';try{const blob=await createPdf(panel);if(!blob.size)throw new Error('PDF vacío');const tournament=clean($('[data-selected-tournament]',panel.closest('.gm-modal'))?.textContent||$('[data-detail-title]')?.textContent||'torneo');downloadPdf(blob,`${safeName(tournament)}-fixture-gmac.pdf`);toast('Fixture PDF descargado en calidad vectorial.')}catch(err){console.error('[GMAC PDF V36]',err);toast('No se pudo generar el PDF del fixture.')}finally{btn.disabled=false;btn.textContent=old||'DESCARGAR FIXTURE PDF ↓'}},true);
syncFixtureButtons();syncFinalizedButtons();setInterval(()=>{syncFixtureButtons();syncFinalizedButtons()},1800);
})();
