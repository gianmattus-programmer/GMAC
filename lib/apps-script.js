const crypto=require('crypto');
function env(){const url=process.env.GOOGLE_APPS_SCRIPT_URL,secret=process.env.SHEETS_API_SECRET;if(!url||!secret)throw new Error('Google Sheets todavía no está configurado.');return{url,secret}}
function safeEqual(a,b){const x=Buffer.from(String(a||'')),y=Buffer.from(String(b||''));return x.length===y.length&&crypto.timingSafeEqual(x,y)}
function requireAdmin(req){const expected=process.env.ADMIN_SECRET;if(!expected)return false;return safeEqual(req.headers['x-admin-secret'],expected)}
async function getAction(action,params={}){const{url,secret}=env();const u=new URL(url);u.searchParams.set('action',action);u.searchParams.set('secret',secret);Object.entries(params).forEach(([k,v])=>{if(v!==undefined&&v!==null&&v!=='')u.searchParams.set(k,String(v))});const r=await fetch(u,{redirect:'follow',headers:{Accept:'application/json'}});const data=await r.json();if(!r.ok||data.ok===false)throw new Error(data.message||'Error de Google Sheets.');return data}
async function postAction(action,payload={},adminSecret=''){const{url,secret}=env();const body={action,secret,...payload};if(adminSecret)body.adminSecret=adminSecret;const r=await fetch(url,{method:'POST',redirect:'follow',headers:{'Content-Type':'text/plain;charset=utf-8','Accept':'application/json'},body:JSON.stringify(body)});const data=await r.json();if(!r.ok||data.ok===false)throw new Error(data.message||'Error de Google Sheets.');return data}
module.exports={getAction,postAction,requireAdmin};
