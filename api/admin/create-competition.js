const{postAction,requireAdmin}=require('../../lib/apps-script');module.exports=async(req,res)=>{if(req.method!=='POST')return res.status(405).json({message:'Método no permitido.'});if(!requireAdmin(req))return res.status(401).json({message:'No autorizado.'});try{const data=await postAction('createCompetition',req.body||{},process.env.ADMIN_SECRET);return res.status(200).json(data)}catch(e){return res.status(400).json({message:e.message})}};
