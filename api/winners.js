const{getAction}=require('../lib/apps-script');module.exports=async(req,res)=>{if(req.method!=='GET')return res.status(405).json({message:'Método no permitido.'});try{const data=await getAction('winners',{competitionId:req.query.competitionId||'',game:req.query.game||''});return res.status(200).json({winners:data.winners||[]})}catch(e){return res.status(503).json({message:e.message})}};
